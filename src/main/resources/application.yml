spring:
  thymeleaf:
    mode: HTML
    encoding: utf-8
    servlet:
      content-type: text/html
    suffix: .html
    cache: false
    cahche:
      chain:
        strategy:
          content:
            enabled: true
            paths: /**
    prefix: classpath:/templates/
server:
  port: 8080
  servlet:
    context-path: /localmachine